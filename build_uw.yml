name: uw3
channels:
  - conda-forge
dependencies:
  - python=3.10
  - numpy
  - mpich-mpicc
  - mpich-mpicxx
  - cython
#   - petsc=3.18.0 ### automatically installed with petsc4py
  - mpi4py
  - petsc4py=3.18.1
  - h5py
  - gmsh
  - pytest
  - ipython # needed for "display" functionality (should be optional !)
  - pyvista # flagged in some tests (should not be needed for tests !)
  - pip
  - jupyterlab # for creating scripts
  - jupytext   # for opening .py files as notebooks

